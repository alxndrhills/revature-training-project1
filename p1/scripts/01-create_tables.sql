create or replace table ERS_REIMBURSEMENT_STATUS (
		reimb_status_id number(5) not null,
		reimb_status varchar2(10) not null
);

create or replace table ERS_REIMBURSEMENT_TYPE (
		REIMB_TYPE_ID number(5) not null,
		REIMB_TYPE varchar2(10) not null
);

create or replace table ERS_USER_ROLES (
	ERS_USER_ROLE_ID number(5) not null,
	USER_ROLE varchar2(10) not null
);






 

